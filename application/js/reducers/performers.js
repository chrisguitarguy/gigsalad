
const initialState = {
    searchQuery: null,
    countryFilter: null,
    stateFilter: null,
    categoryFilter: null,
    performers: []
}

export default function performers(state=initialState, action) {
    // todo
    return state;
}
